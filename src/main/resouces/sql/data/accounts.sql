CREATE TABLE ACCOUNT
(
    ID         BIGINT NOT NULL,
    FIRST_NAME VARCHAR(255),
    LAST_NAME  VARCHAR(255),
    BALANCE    FLOAT8 NOT NULL CONSTRAINT C_POSITIVE_BALANCE CHECK (BALANCE > 0),
    PRIMARY    KEY (ID)
)